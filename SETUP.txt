KHAZAANA CUSTOMER ORDER NOTIFICATIONS — CORRECTED

1. GitHub customer site:
   Replace track-order.html with this version.
   Replace firebase-messaging-sw.js with this version.
   Keep both files in the same customer-site folder as track-order.html.

2. Backend / Render:
   Replace server.js with this version in the GitHub repo connected to Render.
   Render should auto-deploy after the commit.

3. Render Environment:
   Keep FIREBASE_SERVICE_ACCOUNT unchanged.
   Add CUSTOMER_TRACKING_BASE_URL=https://helpkhazaana-eng.github.io/CNC-MENU
   if this is the exact public customer site URL.

4. Android Chrome:
   Open the Track Order page from HTTPS.
   If notification permission is not already granted, the page shows
   "Enable Order Updates". Tap it and choose Allow.

5. Test:
   Customer opens Track Order and enables notifications.
   Admin clicks Accept -> Out For Delivery -> Delivered.
   The customer's Android Chrome should receive the corresponding push notifications.

6. Laptop notifications are not required for this setup.
